# macOS
.DS_Store

# Partial / aborted downloads
*.crdownload

# Build / deploy artifacts (zips, temp deploy bundles)
*.zip
Product/ziuzDgtq
# Duplicate of landing-site/ (extracted from qa-mastery-site.zip)
Product/qa-mastery-site/

# Duplicate file
Resources/final CSS in Web Development (2).pdf

# graphify internals (keep the published outputs, drop machine-local state)
graphify-out/cache/
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/.graphify_labels.json

# Separate platform codebase — has own git repo + own GitHub remote
qa-mastery/

# node + remotion build artifacts
node_modules/
Marketing/06-video/out/
Marketing/06-video/.cache/

# Obsidian vault — commit settings + plugins, ignore per-machine/transient state
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# Raw screen recordings (90MB+ .mov) — sources for 06-video cuts, too big for git
Marketing/Screen recording/
